/**
 * routes/web.ts — 独立网页路由
 *
 * GET /ui/       → 服务前端 Vue 应用（dist/index.html）
 * GET /ui/*      → 服务前端静态资源
 */
import type { Router } from "express";
export declare function registerWebRoutes(router: Router): void;
//# sourceMappingURL=web.d.ts.map